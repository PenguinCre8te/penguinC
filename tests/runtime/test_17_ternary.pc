#import std.console;
int main() {
    int x = 10;
    int result = x > 5 ? 1 : 0;
    console.println_int(result);
    int y = 3;
    int result2 = y > 5 ? 1 : 0;
    console.println_int(result2);
    return 0;
}
