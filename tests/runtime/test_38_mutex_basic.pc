#import std.mutex;
#import std.console;

int main() {
    long m = mutex.create()
    using(m) {
        console.println("inside_using")
    }
    console.println("after_using")
    mutex.destroy(m)
    return 0;
}
