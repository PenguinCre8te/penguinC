#import std.mutex;
#import std.io;

int main() {
    long m = mutex.create()
    using(m) {
        io.println("inside_using")
    }
    io.println("after_using")
    mutex.destroy(m)
    return 0;
}
